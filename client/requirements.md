## Packages
framer-motion | Page transitions and scroll-triggered animations
react-icons | Social media and technology icons
react-hook-form | Form state management
@hookform/resolvers | Zod validation resolver for forms
lucide-react | Generic UI icons

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["var(--font-sans)"],
  display: ["var(--font-display)"],
}
The application uses a dark theme by default, styled with olive green and gold accents.
Contact form submits to POST /api/messages.
